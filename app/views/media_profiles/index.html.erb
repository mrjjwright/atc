<% @media_profiles.each {|m| %>
	<div class="news_item">
		<div class="profiled_media">
			<h2><%=  link_to m.updated_at.strftime("%a %b #{m.updated_at.day.ordinalize}") + " - " + m.title, m %></h2>
				<%
					if not m.video_embed.blank? then
				%> 
					<%= m.video_embed%>
				<%
			else
				%>
				<img src="<%= m.landing.url %>"/>
			<%end%>
		</div>

		<div class="news">
		<%= m.content %>
		</div>
	</div>
	<div style="clear: both;"></div>

<% }%>